build docker:
docker build -t flask-smorest-api .

start docker with on the fly change tracking (windows bash version):
docker run -dp 5005:5000 -w //app -v "//$(pwd)://app" flask-smorest-api

install requirements
pip install -r requirements.txt




flask db init
flask db migrate
flask db upgrade


git status
git commit -a   

git checkout somwhere

git checkout main   - go to back to the main branch
git branche new_branch - create new branch



git checkout main
git merge new_branch